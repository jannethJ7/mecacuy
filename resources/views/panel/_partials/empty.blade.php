<div class="mc-pro-empty">
    <div class="mc-pro-empty-icon">
        <i class="{{ $icon ?? 'ri-inbox-line' }}"></i>
    </div>
    <h3>{{ $title ?? 'Sin registros' }}</h3>
    <p>{{ $message ?? 'Todavía no hay información para mostrar.' }}</p>
</div>
